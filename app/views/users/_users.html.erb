<% if users.any? %>
  <ul class="media-list">
    <% users.each do |user| %>
      <li class="media">
        <div class="media-left">
          <%= user.name %>
          <%= image_tag(group_image?(@user), size: "100x100") %>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>